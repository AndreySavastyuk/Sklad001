# 📦 Система управления складом

Современная веб-система для управления складскими операциями с красивым интерфейсом и мощным API.

## 🏗️ Архитектура

Проект разделен на два компонента:
- **Backend** - FastAPI REST API сервер
- **Frontend** - Современный веб-интерфейс (HTML/CSS/JS)

```
Sklad01/
├── backend/           # API сервер
│   ├── main.py       # Основное приложение FastAPI
│   ├── models.py     # Модели данных SQLAlchemy
│   ├── schemas.py    # Pydantic схемы
│   ├── start.py      # Скрипт запуска
│   ├── requirements.txt
│   └── sklad.db      # База данных SQLite
├── frontend/         # Веб-интерфейс
│   ├── index.html    # Главная страница
│   └── app.js        # JavaScript логика
└── README.md
```

## ✨ Возможности

### 📋 **Задания**
- ✅ Создание, редактирование, удаление заданий
- 🔄 6 статусов: В разработке → Подготовлено → Отправлено → Выполняется → Остановлено → Готово
- 🎨 Цветовая индикация статусов
- 🔍 Поиск и сортировка
- 📊 Автоматическое отслеживание дат
- 📥 Импорт из Excel

### 📦 **Приемка**
- ➕ Регистрация поступлений товаров
- 📝 Полная информация: номер заказа, обозначение, количество, маршрутная карта
- 🏷️ Статусы: Принят, Есть замечания, Проведен в НП
- 🔎 Поиск и фильтрация

### 🗄️ **Архив**
- 🔄 Автоматическая архивация через 7 дней после завершения
- 📜 История изменений каждого задания
- 🔄 Создание новых заданий на основе архивных
- 📊 Полная трассируемость

## 🚀 Быстрый запуск

### 1. Установка зависимостей
```bash
cd backend
pip install -r requirements.txt
```

### 2. Запуск сервера
```bash
# Вариант 1: Через скрипт
python start.py

# Вариант 2: Напрямую через uvicorn
uvicorn main:app --host 0.0.0.0 --port 8000 --reload
```

### 3. Открытие в браузере
- 🌐 **Веб-интерфейс**: http://localhost:8000
- 📚 **API документация**: http://localhost:8000/docs
- 🔧 **Альтернативная документация**: http://localhost:8000/redoc

## 🎨 Дизайн

Современный, адаптивный интерфейс с:
- 🎨 Красивый градиентный дизайн
- 📱 Полная адаптивность для мобильных устройств
- 🌙 Профессиональная цветовая схема
- ✨ Плавные анимации и переходы
- 📊 Интуитивные таблицы и формы
- 🔔 Toast-уведомления

## 🛠️ Технологии

### Backend
- **FastAPI** - современный Python веб-фреймворк
- **SQLAlchemy** - ORM для работы с БД
- **SQLite** - легкая встраиваемая БД
- **Pydantic** - валидация данных
- **Pandas** - работа с Excel
- **Uvicorn** - ASGI сервер

### Frontend
- **Vanilla JavaScript** - без зависимостей
- **Modern CSS** - CSS Grid, Flexbox, CSS Variables
- **Fetch API** - современные HTTP запросы
- **Responsive Design** - адаптивная верстка

## 🔌 API Endpoints

### Задания
- `GET /api/tasks` - Получить список заданий
- `POST /api/tasks` - Создать задание
- `GET /api/tasks/{id}` - Получить задание по ID
- `PUT /api/tasks/{id}` - Обновить задание
- `DELETE /api/tasks/{id}` - Удалить задание
- `GET /api/tasks/{id}/history` - История изменений

### Приемка
- `GET /api/receptions` - Получить список приемок
- `POST /api/receptions` - Создать запись о приемке

### Служебные
- `POST /api/tasks/archive` - Архивировать старые задания
- `POST /api/tasks/import` - Импорт из Excel

## 📋 Примеры использования

### Создание задания
```bash
curl -X POST "http://localhost:8000/api/tasks" \
     -H "Content-Type: application/json" \
     -d '{
       "number": "2023/001",
       "name": "Корпус станка",
       "description": "Изготовление корпуса для токарного станка",
       "status": "в разработке"
     }'
```

### Поиск заданий
```bash
curl "http://localhost:8000/api/tasks?search=корпус&status=готово"
```

### Импорт Excel
```bash
curl -X POST "http://localhost:8000/api/tasks/import" \
     -F "file=@tasks.xlsx"
```

## ⌨️ Горячие клавиши

- `Ctrl + N` - Создать новую запись
- `Ctrl + F` - Фокус на поиск
- `Escape` - Закрыть модальное окно
- `Enter` - Поиск (в поле поиска)
- `Double Click` - Редактировать запись

## 🔧 Настройка

### Изменение порта
```python
# В файле backend/start.py
uvicorn.run("main:app", port=8080)  # Изменить на нужный порт
```

### Настройка базы данных
```python
# В файле backend/models.py
DATABASE_URL = "sqlite:///./custom_path.db"  # Изменить путь к БД
```

### CORS настройки
```python
# В файле backend/main.py
app.add_middleware(
    CORSMiddleware,
    allow_origins=["http://localhost:3000"],  # Добавить разрешенные домены
)
```

## 📊 Мониторинг

Система автоматически логирует все операции:
- 📝 Создание/изменение записей
- 🔍 Поисковые запросы
- 📥 Импорт данных
- 🗄️ Архивирование

## 🐛 Диагностика

Для проверки системы запустите:
```bash
python diagnostic.py
```

## 🤝 Поддержка

При возникновении проблем:
1. Проверьте логи в консоли браузера (F12)
2. Убедитесь, что API сервер запущен на порту 8000
3. Проверьте файлы в папке backend/
4. Убедитесь, что установлены все зависимости

## 📈 Производительность

- ⚡ Быстрая загрузка (< 1 сек)
- 🔄 Автоматическое обновление данных
- 📱 Оптимизация для мобильных устройств
- 💾 Минимальное потребление памяти

## 🔐 Безопасность

- 🛡️ Валидация всех входных данных
- 🔒 Защита от SQL-инъекций через ORM
- 🚫 Защита от XSS через экранирование HTML
- 📝 Логирование всех операций

---

**Разработано с ❤️ для эффективного управления складом** 